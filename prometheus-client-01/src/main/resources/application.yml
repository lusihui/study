spring:
  application:
    name: myfirst

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
#  metrics:
#    tags:
#      application: ${spring.application.name}

  endpoint:
    health:
      enabled: true
      show-details: always


